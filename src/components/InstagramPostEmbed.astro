---
import { fetchInstagramEmbed } from "../lib/socialEmbed";
import { SNS_URLS } from "../data/sns";

interface Props {
  postUrl: string;
}

const { postUrl } = Astro.props;
const embed = await fetchInstagramEmbed(postUrl);
const profileUrl = SNS_URLS.footer.instagram!;
---

<div class="w-full">
  {
    embed ? (
      <Fragment set:html={embed.html} />
    ) : (
      <a
        href={profileUrl}
        target="_blank"
        rel="noopener noreferrer"
        class="flex h-40 items-center justify-center rounded-2xl border border-gray-200 bg-white text-sm transition-colors hover:border-[var(--color-primary)] hover:text-[var(--color-primary)]"
      >
        Instagramで最新の投稿を見る ↗
      </a>
    )
  }
</div>

<script async src="https://www.instagram.com/embed.js"></script>
